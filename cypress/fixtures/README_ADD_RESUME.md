# Add your resume file here for Cypress tests

This directory is where Cypress fixtures are stored. The UpdateNaukriProfile test expects a file named:

  Shashidhar_AgenticAI_Final.docx

Place the actual resume file at:

  cypress/fixtures/Shashidhar_AgenticAI_Final.docx

Notes:
- The `cypress-file-upload` plugin reads files from the fixtures folder by default.
- Do NOT commit sensitive/personal documents to public repositories. If this repo is public, instead provide a CI-accessible secure artifact or mock file.
- Alternatively, update the test to point to a non-sensitive test file present in fixtures.
